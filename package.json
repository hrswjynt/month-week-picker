{
  "name": "non-standard-input",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/ && cp -r icons dist/",
    "package": "npm run build && cd dist && zip -r ../month-week-picker.zip .",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "@tailwindcss/vite": "^4.1.4",
    "svelte": "^5.43.8",
    "tailwindcss": "^4.1.4",
    "vite": "^7.2.4"
  }
}